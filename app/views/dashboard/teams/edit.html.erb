<%= simple_form_for [:dashboard, @team] do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.input :activity, label: 'Sport' %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>